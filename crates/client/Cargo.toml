[package]
name = "vscfreedev-client"
version = "0.0.1"
edition = "2024"

[dependencies]
vscfreedev-core = { workspace = true }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "io-util", "sync", "time", "fs", "process"] }
anyhow = "1"
thiserror = "2"
ssh2 = "0.9"

[dev-dependencies]
assert_cmd = "2.0"
predicates = "3.0"
tempfile = "3.8"
rand = "0.8"
